# Данные для воспроизводимости Northern Development Index (NDI)

## 📊 Описание

Этот набор данных позволяет полностью воспроизвести расчёт **Northern Development Index (NDI)** для 128 арктических населённых пунктов России.

**NDI** — композитный индекс устойчивости и привлекательности арктических территорий, разработанный для анализа качества жизни и выявления факторов удержания/оттока населения.

---

## 📂 Структура данных

### ⭐ **1. Итоговый результат** (главный файл)

**`ndi_scores.csv`** — финальные оценки NDI для всех 128 НП

| Колонка | Описание | Диапазон |
|---------|----------|----------|
| `settlement_id` | ID населённого пункта | 1-128 |
| `settlement_name` | Название НП | текст |
| `region_name` | Регион РФ | текст |
| `ndi_score_100` | **NDI (баллы 0-100)** | 0-100 |
| `ndi_rank` | Ранг по NDI (1 = лучший) | 1-128 |
| `poad_score_100` | Качество жизни (баллы) | 0-100 |
| `market_score_100` | Доступность рынков (баллы) | 0-100 |
| `consumption_score_100` | Потребление (баллы) | 0-100 |
| `accessibility_score_100` | Доступность инфраструктуры (баллы) | 0-100 |
| `climate_score_100` | Климат (баллы) | 0-100 |
| `mobility_score_100` | Мобильность (баллы) | 0-100 |

**Объём:** 128 строк × 33 колонки ≈ **34 КБ**

---

### 📊 **2. Компоненты NDI** (`components/`)

NDI рассчитывается как взвешенная сумма 6 компонентов:

```
NDI = 0.35×POAD + 0.20×Market + 0.15×Consumption +
      0.15×Access + 0.10×Climate + 0.05×Mobility
```

#### **A) `poad_attractiveness_v1.csv`** — Качество жизни (35% веса)
- **Источник:** Агрегация 93 показателей POAD (медицина, образование, культура, экология, экономика)
- **Метод:** Корреляционный анализ с населением → топ-10 факторов → нормализация 0-1
- **Объём:** 128 строк × 4 колонки ≈ 3 КБ
- **УЖЕ СУЩЕСТВУЕТ** в `results/data/`

#### **B) `market_access_municipality.csv`** — Доступность рынков (20% веса)
- **Источник:** Росстат, БД муниципальных образований (2024)
- **Описание:** Экономическая интеграция муниципалитета
- **Объём:** 2,571 строк × 3 колонки ≈ **42 КБ**

#### **C) `consumption_municipality_2024.csv`** — Потребление (15% веса)
- **Источник:** Сбербанк транзакции (июль-декабрь 2024)
- **Описание:** Среднее потребление / прожиточный минимум региона (clip at 2.0)
- **Объём:** 12,578 строк × 4 колонки ≈ **622 КБ**

#### **D) `accessibility_scores.csv`** — Доступность инфраструктуры (15% веса)
- **Источник:** Расчёт минимальной дистанции до 12 крупнейших хабов
- **Описание:** Транспортная доступность (обратная функция от расстояния)
- **Объём:** 128 строк × 4 колонки ≈ 3 КБ
- **УЖЕ СУЩЕСТВУЕТ** в `results/data/`

#### **E) `climate_monthly_2023_2024.csv`** — Климат месячный (10% веса)
- **Источник:** ВНИИГМИ-МЦД (2023-2024)
- **Описание:** Среднемесячная температура для расчёта зима/лето/амплитуда
- **Объём:** 3,072 строки × 6 колонок ≈ **88 КБ**

#### **F) `climate_yearly_hdd.csv`** — Климат годовой (10% веса)
- **Источник:** Агрегация месячных данных
- **Описание:** HDD (Heating Degree Days) — мера холода для затрат на отопление
- **Объём:** 256 строк × 4 колонки ≈ **8 КБ**

#### **G) `mobility_index_municipality.csv`** — Мобильность населения (5% веса)
- **Источник:** Сбербанк геолокация (2024)
- **Описание:** Средняя дальность перемещения населения в км
- **Объём:** 297 строк × 3 колонки ≈ **32 КБ**

---

### 📖 **3. Справочники** (`dictionaries/`)

#### **A) `dict_settlements.csv`** — 128 арктических НП
- Колонки: `settlement_id`, `settlement_name`, `settlement_type`, `region_id`, `municipality_up_id`, `municipality_down_id`
- Объём: **6 КБ**

#### **B) `dict_regions.csv`** — 10 регионов РФ
- Колонки: `region_id`, `region_name`
- Объём: **0.5 КБ**

#### **C) `dict_municipalities.csv`** — 192 муниципалитета
- Колонки: `municipality_id`, `municipality_name`, `region_id`
- Объём: **13 КБ**

#### **D) `dict_indicators.csv`** — 93 показателя POAD
- Колонки: `indicator_code`, `indicator_name`, `acronym`, `calculation_method`, `normalization_method`
- Объём: **15 КБ**
- **УЖЕ СУЩЕСТВУЕТ** в `results/data/`

#### **E) `meta_settlement_attributes.csv`** — Атрибуты НП
- Колонки: `settlement_id`, `is_arctic`, `is_remote`, `is_special`, `is_suburb`
- Объём: **3 КБ**

---

## 💾 **Общий объём данных:** ~850 КБ (0.83 МБ)

---

## 🔧 Как воспроизвести NDI

### **Вариант 1: Быстрый старт (использовать готовый результат)**

```python
import pandas as pd

# Загрузить итоговый NDI
ndi = pd.read_csv('data/ndi_scores.csv')

# Топ-10 по NDI
print(ndi.head(10)[['settlement_name', 'region_name', 'ndi_score_100', 'ndi_rank']])
```

---

### **Вариант 2: Пересчитать NDI из компонентов**

```python
import pandas as pd

# 1. Загрузить компоненты
poad = pd.read_csv('data/components/poad_attractiveness_v1.csv')
market = pd.read_csv('data/components/market_access_municipality.csv')
consumption = pd.read_csv('data/components/consumption_municipality_2024.csv')
accessibility = pd.read_csv('data/components/accessibility_scores.csv')
climate_monthly = pd.read_csv('data/components/climate_monthly_2023_2024.csv')
climate_yearly = pd.read_csv('data/components/climate_yearly_hdd.csv')
mobility = pd.read_csv('data/components/mobility_index_municipality.csv')

# 2. Реализовать логику из scripts/create_ndi_view.sql
# (нормализация, заполнение пропусков, weighted sum)

# 3. Проверить совпадение с ndi_scores.csv
```

---

### **Вариант 3: Загрузить в PostgreSQL и создать view**

```bash
# 1. Загрузить данные в PostgreSQL
python scripts/load_csv_to_postgres.py

# 2. Создать view
psql -U bot_etl_user2 -d platform -f scripts/create_ndi_view.sql

# 3. Запросить данные
psql -U bot_etl_user2 -d platform -c "SELECT * FROM sberindex.vw_ndi_calculation LIMIT 10;"
```

---

## 📚 Источники данных

| Компонент | Источник | Период |
|-----------|----------|--------|
| **POAD** | Сбербанк Data Science (93 показателя) | 2019-2024 |
| **Market Access** | Росстат, БД МО | 2024 |
| **Consumption** | Сбербанк транзакции | июль-дек 2024 |
| **Accessibility** | Расчёт по дистанциям | 2024 |
| **Climate** | ВНИИГМИ-МЦД | 2023-2024 |
| **Mobility** | Сбербанк геолокация | 2024 |

---

## 🔍 Валидация данных

### **Проверка целостности:**

```bash
# Количество НП должно быть 128
wc -l data/ndi_scores.csv  # 129 строк (включая заголовок)

# Проверка пропусков
python -c "import pandas as pd; print(pd.read_csv('data/ndi_scores.csv').isnull().sum())"
```

### **Тест воспроизводимости:**

```python
# Сравнить ручной пересчёт с ndi_scores.csv
import pandas as pd
import numpy as np

ndi_from_file = pd.read_csv('data/ndi_scores.csv')
ndi_recalculated = recalculate_ndi()  # ваша функция

# Допустимая погрешность: 0.01 балла
assert np.allclose(ndi_from_file['ndi_score_100'],
                   ndi_recalculated['ndi_score_100'],
                   atol=0.01)
```

---

## 📜 Лицензия

Данные предоставлены для исследовательских целей в рамках **Хакатона Сбербанка по устойчивому развитию арктических территорий (ноябрь 2025)**.

При использовании данных просьба ссылаться на источник:

```
Northern Development Index (NDI) для арктических территорий РФ.
Сбербанк Data Science, 2025.
GitHub: [ссылка на репозиторий]
```

---

## 🤝 Контакты

Вопросы по данным и методологии: [ваш контакт]

---

## 📝 История изменений

- **2025-11-14:** Первая версия экспорта (10 файлов, 0.83 МБ)
- **TODO:** Добавить данные миграции населения 2020-2024
- **TODO:** Добавить прогнозную модель устойчивости
