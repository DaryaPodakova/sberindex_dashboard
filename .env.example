# PostgreSQL Connection
# Эти параметры используются для подключения к базе данных PostgreSQL
# при воспроизведении pipeline генерации данных

DB_HOST=localhost
DB_PORT=5432
DB_NAME=platform
DB_USER=bot_etl_user2
DB_PASSWORD=your_password_here

# Python Path
# Путь к корневой директории проекта
PYTHONPATH=/path/to/sberindex_dashboard

# Optional: Data sources paths
# Пути к исходным данным (если используете Вариант Б - полное воспроизведение)
DATA_DIR=/path/to/data
SBERINDEX_EXCEL=/path/to/Data_SberIndex_POAD.xlsx
CONSUMPTION_PARQUET=/path/to/consumption.parquet
MARKET_ACCESS_PARQUET=/path/to/market_access.parquet
MOBILITY_INDEX_EXCEL=/path/to/mobility_index.xlsx
